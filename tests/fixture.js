{
  vasa: 'eto boroda';
  fn1: () => {
    x = {};
    y = { };
    console.log('fn1');
  };
  fn2: () => {
    console.log('fn2');
    console.log({ innerHeight: 322 });
  };
}

const curlyRegions = [[start, end], '...'];

[1].reduce((acc, el) => {
  console.log(acc);
  console.log(el);
  return el <acc ? el : acc; 
});

"simple quote" 
'string [ 1,2,3] "non capturing other string" ` ' `multi
line with [ 1,2,3 ] {} (hello)
quote`

"single quote 'inside' and `multiline` with { object:322 } "
x = <div class="vasa" prop={ggwp}>Some text</div>
nestedTags = (
  <bgovider value={someValue}>
    <NavBar>
      <div className="title">How are you?</div>
    </NavBar>
    <Dashboard
      id="322"
      opened
      data={data}
    />
  </bgovider>
)

dot = (
  <Provider.Context value={someValue}>
    <NavBar>
      <div className="title">How are you?</div>
    </NavBar>
    <>
      
    </>
  </Provider.Context>
)

nestedDiv = (
  <div value={someValue}>
    <div>
      <div className="title">How are you?</div>
    </div>
  </div>
)

  const z=$42vas31$
  const escapeChars = `
  it doesn\`t matter
  `
  const anotherTry = '\'' || '\\'
